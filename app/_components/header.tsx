export default function Header(){
    return (
        <div className="flex w-full px-8 text-lg text-center justify-center font-manrope uppercase font-semibold text-[#ffe135] tracking-wide">
            INVEST, HOLD, AND REAP 100% ANNUAL PERCENTAGE YIELD
        </div>
    )
}